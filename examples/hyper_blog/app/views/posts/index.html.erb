<h1><%= @title %></h1>

<ul>

<% @posts.each do |post| %>
  <li>
  	<span class="date"><%= post.created_at.strftime("%m-%d-%y") %></span>
  	<%= link_to post.title, post %>
  	<%= image_tag post.user.avatar(25) %>
  </li>
<% end %>

</ul>

<br />
